import styled from 'styled-components';
import tw from 'twin.macro';

export const Banner = styled.div`
    ${tw`relative w-full h-[623px]`}
`

export const Background = styled.div`
    ${tw`relative w-full h-[632px]`}
`